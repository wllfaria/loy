import "./other" as K with {foo as bar, bar as foo};

pub type Foo = interface {
    fun a();
}

fun foo<T>(args: usize) {
    if ((1 * 4 + 3) - --3 + 2,2,3) {
        [1,2,3];
    } else if true {
        ();
    } else {
        [];
    }
}